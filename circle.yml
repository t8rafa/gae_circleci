dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
  	- nosetests --with-gae --gae-lib-root ../google_appengine
    - coverage run --source=main.py -m nose2.__main__ -v
  post:
    - coverage html -d $CIRCLE_ARTIFACTS